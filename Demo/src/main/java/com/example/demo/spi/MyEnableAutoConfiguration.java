package com.example.demo.spi;

public class MyEnableAutoConfiguration {
    // 全路径com.example.demo.spi.MyEnableAutoConfiguration作为factories文件中的key
    // 因为 SpringFactoriesLoader.loadFactoryNames(MyEnableAutoConfiguration.class)指定了这个类
}
